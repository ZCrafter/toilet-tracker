# docker-compose.yml
version: '3.8'
services:
  web:
    build: .
    container_name: toilet-tracker
    ports:
      - "5001:5001"
    volumes:
      # persist sqlite file on the host
      - ./data:/app/data
    restart: unless-stopped
